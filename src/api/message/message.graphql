type Message {
	id: ID!
	content: String!
	statusMessage: Boolean!
	sender: User!
	createdAt: String!
	updatedAt: String!
}

input NewMessage {
	chatId: ID!
	senderId: ID!
	content: String!
	statusMessage: Boolean!
}

extend type Mutation {
	newMessage(input: NewMessage!): Message
}
